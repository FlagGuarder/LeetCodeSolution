package Hash.Solution137;

import java.util.Arrays;

public class Solution {
    public int singleNumber(int[] nums) {
        long trubleNums = Arrays.stream(nums).sum() * 2;
        for (int num : nums){

        }
        return 0;
    }
}
